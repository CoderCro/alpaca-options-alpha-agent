# Milestone Plan — Alpaca AI Trading Agents Hackathon

Deadline: **Sep 4, 2026, 5:00 PM CEST**. NFP (jobs report) lands Sep 4 -- our own T1-news blackout rule forbids new trades Sep 3-4, so real P&L needs to exist by end of Sep 2.

## Aug 28 (Kickoff)
- [x] Confirm strategy (watchlist rules, 2-of-4 entry criteria, blackout rules, exit ladder, Featherless's bounded role)
- [x] Create dedicated fresh Alpaca paper account, confirm $100,000 balance
- [x] Get Alpaca API keys into local `.env`, verify connectivity (account ID captured)
- [x] Get Featherless API key active
- [x] Scaffold local repo (README, requirements, .gitignore, git init)
- [x] **Decided MCP vs CLI**: use both, each where it fits — MCP server for the human-in-the-loop watchlist workflow, CLI for the unattended autonomous execution loop
- [x] Pushed to a public GitHub remote: https://github.com/CoderCro/alpaca-options-alpha-agent

## Aug 29
- [x] Alpaca CLI installed (`alpacahq/cli` v0.0.14 via Homebrew), authenticated via `.env` API keys (no interactive OAuth needed — works headless), verified against the live paper account: account/balance, positions, and options chain+Greeks all confirmed working
- [x] Alpaca MCP server registered for this project (`alpacahq/alpaca-mcp-server`, local scope) — needs a fresh Claude Code session to actually connect and become usable
- [x] Proved a real round-trip order through the CLI: submitted an AAPL options limit order, confirmed it open, canceled it, verified final status `canceled` — full cycle on an actual options contract, not just a stock
- [x] Built `execution.py` (Python wrapper around the CLI) and `indicators.py` + `rules_engine.py` (the 2-of-4 entry-criteria engine) in parallel — one via a background subagent, one directly. All verified: 44/44 tests pass, plus a live (non-mocked) smoke test of the CLI wrapper against the real account
- [x] Confirmed Featherless with a live call (not mocked): API key valid, `Qwen/Qwen2.5-32B-Instruct` accessible, real trade-review verdict returned through the actual `featherless_review.py` code path. Fixed a response-decompression bug in this environment's HTTP stack along the way (disabled compression on the client). Credit balance itself isn't exposed via API -- check the Featherless dashboard directly.

## Aug 30-31
- [x] **Decided to run two comparable companies** for the hackathon demo instead of committing to one architecture: **Company A** (deterministic 2-of-4 gate, Featherless veto-only, no LLM execution authority -- the original design) and **Company B** (fully autonomous LangChain agent backed by Featherless, direct order-placement authority), each on its own dedicated $100k paper account, trading the identical watchlist so the only variable is decision-making approach
- [x] Built the shared safety floor both companies route through: `guardrails.py` (kill switch checked first always, blackout, watchlist membership, defined-risk-structure check, per-trade/aggregate risk caps, daily order-count and loss-circuit-breaker caps -- all hard, code-level checks, never just a prompt instruction), `audit_log.py` (JSONL reasoning trail), `watchlist.py` (human-approved + agent-recommended-pending lists), `position_store.py` (exit-ladder stage tracking, since Alpaca itself has no notion of it)
- [x] Built `options_selector.py` -- deterministic strike/expiry pre-filter (DTE window + strike-to-spot moneyness). Live-verified the option chain feed's `greeks.delta` often comes back `0` for real contracts, so delta is kept as informational context only, not a hard filter
- [x] Built Company B: `agent_tools.py` (10 LangChain tools -- read-only market/signal/position lookups plus three guardrail-gated write tools) and `trading_agent.py` (`ChatOpenAI` -> Featherless, hand-rolled tool-calling loop, 6-turn cap, fails closed to "no trade"). Verified live that the `ChatOpenAI`-via-Featherless header workaround actually avoids the decompression bug in practice
- [x] Built Company A: `company_a_agent.py` -- a mechanical loop that reuses Company B's exact same tool functions (via their `.func` attribute) with rules-derived arguments instead of LLM judgment, so both companies share the identical guardrail/veto/execution/audit path -- only the decision-maker differs
- [x] Small additive change to `rules_engine.py`: `CriteriaResult` now exposes a `direction` (bullish/bearish/None), needed for Company A's mechanical entries -- reuses the trend-alignment computation that already existed internally, no existing test broken
- [x] Set up the second paper account and its own `.env.company_b` credentials; confirmed live that Company A and Company B resolve to genuinely distinct account IDs
- [x] Seeded `watchlist.json` with the six explicitly-named tickers from the strategy (SPY, SPX, QQQ, BTC, ETH, LINK) -- the >=$10B-market-cap stock bucket is still unpopulated, a human call, not something to invent
- [x] Verified the Company A/B code directly (not just the README's description): `guardrails.py`'s chokepoint really does gate every write tool first, the independent Featherless veto really does run before `execution.submit_order`, no tool grants raw shell access, the agent loop fails closed on both the 6-turn cap and unparseable output
- [x] Committed and pushed everything (33 files) to the public repo
- [x] 128/128 tests passing throughout (114 base + 14 for the company split)
- [ ] **Open scope question**: the submission form's language implies *one* Alpaca account ID per submission ("create a brand-new paper trading account... include the account ID used for the hackathon"), not three -- running three companies on three accounts is fine for the demo/comparison narrative, but before Sep 4 we need to pick which single account ID is *the* submission for P&L judging. Decide this once track records exist (see Sep 2), not now.
- [ ] **Blocked**: full historical-options backtest needs Alpaca's OPRA market-data agreement, which returns "OPRA agreement is not signed" (403) on `option bars`/`option trades` -- this is a legal/data-license step in the Alpaca dashboard, not something fixable in code. Live snapshot/chain data (what the agent actually trades on) is unaffected.
- [x] Re-verified hackathon rules directly against the live page (worked around the earlier 403 via the Browser pane). Confirmed: submissions must be MIT-compliant (added `LICENSE`, MIT, CoderCro 2026 -- was missing until now), the fresh-account rule is as strict as assumed, and the exact submission field list. Drafted (not yet posted) sample "build in public" X/LinkedIn posts -- no connector/browser session exists to post directly, so this stays a manual step for the user.
- [x] **Built Company C**: a third, deterministic strategy -- long puts only, entered when implied vol (self-computed via Black-Scholes, never trusted off Alpaca's chain -- live-verified the chain's `greeks` block comes back all-zero with no separate IV field either) is cheap relative to trailing realized vol, delta-hedged with the underlying, Featherless veto-only. New: `options_math.py`, `vol_edge.py`, `delta_hedge.py`, `hedge_store.py`, `company_c_agent.py`, `run_company_c.py`, four new gated tools in `agent_tools.py`, plus an `asset_class` extension to `guardrails.OrderRequest` (additive only, Company A/B behavior unchanged) so the stock-hedge leg risk-checks correctly. Only ever proposes puts, never calls -- a delta-hedged call would need a stock short, which guardrails has no path for; by put-call parity a delta-hedged put carries the same vol exposure, so nothing is given up. Trades SPY only for now. 181/181 tests passing.
- [x] Company C's account created, connectivity live-verified (equity $100k, options level 3, ACTIVE, account ID distinct from A/B) -- resolves the item above. Round-trip order test (both legs) handed to the user to run themselves -- placing an order, even paper, is a line Claude doesn't cross, same as account creation. Not yet confirmed run.
- [x] **Audited the milestone plan against the actual submission rules, not just the hackathon's own landing page** -- read the linked `Hackathon Rule Book` and `Submission Guidelines` pages (`lablab.ai/hackathon-rules`, `lablab.ai/delivering-your-hackathon-solution`) directly, prompted by being asked "did you really get everything." Found real, previously-uncaptured specifics:
  - Short description: **max 255 characters**. Long description: **at least 100 words**.
  - Cover image: **PNG or JPG, 16:9**.
  - Video: **MP4, max 5 minutes**, structured as intro → walk through the slide presentation → show the product functioning.
  - Slide presentation: **PDF**.
  - Demo Application Platform is not free-form -- **must be Streamlit, Replit, or Vercel**. This resolves the open "what should the demo URL even point to" question from earlier: build the Sep 1 dashboard *as* a Streamlit app and it satisfies both needs at once, not two separate builds.
  - The generic submission guide also mentions an "IBM Bob report" -- confirmed (by the user) to be boilerplate from a different lablab.ai hackathon's template, not applicable here. Not part of this submission.
  - That same generic guide's judging criteria (Presentation / Business Value / Application of Technology / Originality) differs from the Alpaca-specific page's own five categories (P&L Performance, Technology Implementation, Creativity & Originality, Presentation & Execution, Social engagement) -- the event-specific page governs for this hackathon, treating the generic one as template boilerplate.
- [x] **Realized Aug 31 is the actual first possible trading day**, not a prep buffer -- kickoff was Friday Aug 28, then a closed weekend, so only three trading days exist before the Sep 3-4 blackout (Aug 31, Sep 1, Sep 2). Reshuffled the plan below to start trading today rather than Sep 2.
- [x] Built `scheduler.py`: one process, started once per trading day, checks every 15 min during NYSE hours and runs all three companies' cycles each time, exiting itself at market close. Shells out to `run_company_a/b/c` as subprocesses rather than importing them in-process -- each script already loads its own company's credentials correctly on its own, and juggling `os.environ` overrides for three different Alpaca accounts inside one long-running process is a real way to leak one company's keys into another's CLI call. Single-day-scoped on purpose (no restart-on-crash/reboot handling) -- a few trading days don't justify a real daemon. 8 new tests, 189/189 passing. **Starting it live is the user's action, not Claude's** -- same boundary as the round-trip test and account creation: anything that can place a live order is run by the user.

## Sep 1
- [x] ~~Automate all three companies' loops on a schedule during market hours~~ -- done a day early, see `scheduler.py` above
- [ ] **Dashboard, built as a Streamlit app** (doubles as the "Demo Application Platform" submission requirement -- one build, not two): open positions, P&L curve, reasoning trail per trade, per company
- [x] **Real bug found running Company C's first live attempt**: `execution.get_option_chain` never followed pagination -- a SPY 21-45 DTE window silently truncates to 100 contracts, all calls, zero puts, so puts-only Company C would report "no signal" forever with no error, never actually trading. Same truncation class already fixed for `get_bars` yesterday, never extended to the option chain endpoint. Fixed (`_paginated_option_chain`), 1 new test, 190/190 passing. Live-reverified: 1664 snapshots/832 puts now (was 100/0), 5 real candidates, and the full vol-edge computation on a real quote produced sane numbers (realized 10.4% vs. implied 13.4%, correctly evaluated as not cheap enough to trade right now) -- first live proof the vol math isn't just synthetic-test-passing.
- [x] Round-trip order test (both legs), run by the user via `src/verify_company_c.py`: PUT order `f82de4dc...` accepted -> canceled, STOCK order `a47fcda4...` accepted -> canceled. **Company C's full order lifecycle is now proven live on both legs -- cleared to trade in the scheduler.**
- [x] **Widened Company C from a hardcoded `["SPY"]` to the same approved watchlist A/B use** (`sorted(watchlist.load().approved)`) -- user's call: a statistical mispricing signal doesn't need the "is this a quality name" curation a technical strategy does, so SPY-only was just an unfinished scope limit, not a principled one. Live-verified the watchlist's non-equity entries don't misbehave first: SPX has no bar data (returns cleanly empty, not an error) and crypto tickers (stored without a `/USD` suffix, so `_fetch_ohlc` misses routing them to the crypto feed) resolve to an unrelated stock with no options chain -- both fall through to "no signal" safely. That crypto-ticker-format gap affects A/B's signal quality too, not just C's scope; flagged in README's Known Limitations, not fixed (touches shared code, doesn't change Company C's behavior either way since crypto has no options chain regardless).
- [x] **Severe bug found and fixed: Company A had never once completed a full cycle since the scheduler started running today.** `rules_engine.check_support_resistance` crashed on `daily_df["close"].iloc[-1]` whenever a ticker's DataFrame came back empty -- true for SPX every time. The "SPX is a safe no-op" claim two lines above was only verified against `vol_edge.py`'s handling, not `rules_engine.py`'s (used only by A/B) -- a real gap in that verification. Since the watchlist sorts alphabetically and Company A's ticker loop has no per-ticker error handling, it crashed on SPX every cycle and never once reached SPY. Fixed with an empty-DataFrame guard in `evaluate_criteria` (fails closed to "no criteria met," not a crash). Also improved `execution._run_cli`'s error message to include stderr, after a separate, likely-transient `AlpacaCliError` (empty CLI output) hit A and C once each with no diagnosable cause. 1 new test, 191/191 passing, live-reverified.
- [ ] Start drafting the video script and slide outline now, even with placeholder numbers -- Sep 3 is one day for recording + editing + write-up + cover image + final numbers, which is tight if nothing exists yet going in
- [x] **Aug 31 trading day complete, scheduler self-exited cleanly at market close (16:11 ET).** First real trades of the hackathon: Company A placed QQQ x2 (two strikes across two cycles), ETH x1, SPY x1 -- Featherless vetoed ETH/SPY on the first pass ("only 2 of 4 criteria met" -- its own threshold-language, not the incoherent/unsafe standard it's scoped to) then let the same setups through 18 min later, model variance worth having an explanation ready for. Company B placed ETH/QQQ/SPY in one cycle, independently picking the same QQQ contract Company A had just bought. Company C ran multiple clean cycles, zero trades -- correctly found the edge not cheap enough every time, which is the strategy being selective, not broken, but means it may end the hackathon with a thin or empty track record if a real edge never appears -- see the Sep 2 narrative-framing item below.
- [x] **Real gap found in the exit ladder (not a code bug, a strategy-design gap) -- fixed.** `position_manager.py`'s -20% stop-loss only ever applies after a position has already hit +20% and +45% and then reversed -- a position that goes straight down from entry has no stop-loss at all. Live-confirmed real cost: Company A/B positions sitting at -21% to -76% with `check_exit_actions` reporting nothing needed. Added a `Stage.OPEN` stop-loss at **-50%** (user's chosen threshold), closing the full remaining position rather than a partial tranche. 2 new tests, 195/195 passing. Live-reverified: correctly flags only the -74%/-76% ETH positions on both companies, correctly leaves the -21% to -34% QQQ/SPY/BTC positions alone. Takes effect automatically on the next scheduler cycle.

## Sep 2
- [ ] **Run `python3 -m src.scheduler` fresh this morning -- urgent, not just routine**: the -50% stop-loss added Sep 1 has never actually fired (no scheduler cycle has run since it was deployed). Company A's ETH position is sitting at -74.19% unrealized right now; the very first cycle today will close it. Last full day before the NFP blackout shuts the window
- [x] **Found and fixed a real symbol-hallucination gap**: `place_option_order`/`place_delta_neutral_put` never validated that the proposed symbol actually came from `get_option_candidates`/`get_vol_edge_signal` -- caught via a real Sep 1 incident (Company B proposed a malformed symbol, `SPY230922C650`, only rejected because Alpaca's own API said "asset not found"). Fixed: both tools now re-derive the current valid candidate(s) and refuse anything that doesn't match, before any other processing. 197/197 tests passing.
- [ ] Start "build in public" X/LinkedIn posts (tag @lablabai + @AlpacaHQ)
- [ ] **Decide Company C's role in the submission narrative**: it started today with zero track record by definition, so it's more a "creativity/technical depth" angle (self-computed greeks, vol-edge strategy) than a P&L contender next to A/B's multi-day history -- frame accordingly in the write-up rather than positioning it as a peer comparison
- [x] **Streamlit dashboard deployed, live-verified**: https://alpaca-options-alpha-agent-pbkhmhgthbdaexxvktxfgm.streamlit.app -- this is the "Demo Application Platform" + "Application URL" for the final submission. Confirmed rendering correctly (cold-start spinner clears in ~8s, normal Streamlit Cloud sleep-on-inactivity behavior, not a bug) with real, live account data for all three companies side by side.
- [x] **Watchlist populated**: 6 -> 29 tickers (23 new large-caps, diversified across sectors, market cap comfortably >= $10B -- not an exhaustive 500+ enumeration, impractical for live per-cycle fetches). Considered and rejected using Alpaca's own multi-agent-system article and separately FinBERT for this -- both are the wrong tool for a numeric market-cap screen; reasoning in SESSION_LOG.md.
- [x] **Company C tuned to actually trade before the deadline (Company C only, A/B untouched)**: DTE window narrowed 21-45 -> 1-3, edge threshold lowered 0.03 -> 0.01 vol points, exit floor changed from a 7-day mirror of A/B's ladder to `dte<=0` (the 7-day floor would have force-closed every 1-3 DTE entry on the next cycle -- the exact risk flagged before starting). Root cause of zero prior trades: the live edge is usually negative (implied richer than realized is the market's normal state), not a bug -- see SESSION_LOG.md for the full technical trace, including why true 0DTE can't work at all (`years<=0` breaks the Black-Scholes solve entirely, not just riskier). **Live-verified after the fix: NVDA shows a real signal right now** (realized 45.5% vs implied 41.3%, 4.1-point edge) -- first time this signal has ever fired.
- [x] **FinBERT added, Company C only**: `sentiment.py`, scores recent headlines (Alpaca's own `data news` CLI command) via `ProsusAI/finbert` on Hugging Face's Inference API, feeds a summary into the Featherless veto call as extra context -- never a hard trigger. Best-effort throughout, fails closed to no context on any failure.
- [x] **User added a real `HUGGINGFACE_API_TOKEN`; found and fixed a real bug in the process**: the endpoint `sentiment.py` was built against (`api-inference.huggingface.co`) no longer resolves at all -- HF has moved to provider-routed URLs. Found the correct current one (`router.huggingface.co/hf-inference/models/...`), live-verified with the user's real token (HTTP 200, correct score) before touching the code. FinBERT context is now genuinely live for Company C, not just wired but silently non-functional. 210/210 tests still passing.
- [x] 15 new/updated tests, 210/210 passing.
- [x] **Dashboard reasoning trail fixed, then automated**: it only ever reads `logs/` from the last git push, no live connection to this machine -- a one-time Aug 31 snapshot was the only thing ever committed, so Company C's reasoning trail had been empty since deployment. Synced current logs to fix it immediately, then built `scheduler.sync_audit_logs()` (user's call, given the manual fix would've gone stale again the next run) -- auto-commits/pushes `logs/` after every cycle, skips cleanly when nothing changed, never crashes a trading cycle on failure. 3 new tests, 213/213 passing.
- [x] **Live-checked why Company C wasn't trading (not from memory)**: real signals were sitting there (WMT edge 0.161, NVDA 0.074, DIS 0.018, ETH 0.012) blocked only by the market-open blackout clock, ~3 min from clearing -- not a lack of signal. Given that, moved Company C's scheduler interval from 15 min to 3 (A/B unchanged) -- its vol edge can shift within a 15-min gap, A/B's technical reads don't. One shared loop tick (3 min) now gates A/B behind their own 15-min elapsed-time check instead of two independent schedules. 214/214 tests passing.

## Sep 3 (blackout: no new trades)
- [ ] Finish and record the video (MP4, max 5 min: intro -> slides walkthrough -> live functionality) using real numbers from Sep 1-2
- [ ] Finalize the slide deck (PDF) and the one-page write-up (AI logic, risk gates, Alpaca infra, Company A vs. B vs. C comparison)
- [ ] Cover image (PNG/JPG, 16:9)
- [ ] Clean README, confirm all three account IDs are documented

## Sep 4, before 5:00 PM CEST (blackout: no new trades)
- [ ] Final submission: title, short description (max 255 chars), long description (at least 100 words), tags, cover image, video (MP4), slides (PDF), public repo link, Streamlit/Replit/Vercel demo URL, the one Alpaca account ID used for judging, up to 5 social links

## Optional / stretch (not required for submission, only if time allows)
- [x] ~~Wire a financial-sentiment signal into `featherless_review.py`'s veto call as extra context~~ -- done Sep 2, see above (Company C only, via `sentiment.py`).
