# Company C's dedicated Alpaca paper account (the delta-neutral vol-edge
# agent). Loaded on top of the base .env -- only the Alpaca keys need to
# differ; Featherless credentials are shared from .env since they're not
# tied to a specific brokerage account.
#
# Per the hackathon rules, this must be a brand-new account created just for
# this hackathon (start balance $100,000) -- a reused account is not
# eligible for judging. See MILESTONES.md/SESSION_LOG.md.
ALPACA_API_KEY=
ALPACA_SECRET_KEY=
