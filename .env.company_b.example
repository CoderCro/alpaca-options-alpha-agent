# Company B's dedicated Alpaca paper account (the fully-autonomous LangChain
# agent). Loaded on top of the base .env -- only the Alpaca keys need to
# differ; Featherless credentials are shared from .env since they're not
# tied to a specific brokerage account.
ALPACA_API_KEY=
ALPACA_SECRET_KEY=
